# 验证记录

- 合成器12项测试通过：四分支输出、同种子复现、种子变化、裁切拒绝、cover裁脸拒绝、缺脸框拒绝、群像框、文字遮脸拒绝、素材遮脸拒绝、透明标题、超长文字、EXIF方向和保存配方路径复用（部分为同一测试中的多个断言）。
- 15张参考图片均按清单校验SHA256并导入。
- 使用几何占位图渲染并打开检查了四个分支的基础布局；没有把占位图作为风格成片交付。
- SKILL.md前置YAML与agents/openai.yaml已通过Ruby YAML解析与入口字段检查。本机缺少PyYAML，官方quick_validate.py因依赖缺失未能运行完成；没有将其记为通过。
- 现已用用户上传的咖啡店人像完成三轮实际制作与视觉检查：原像素人像＋AI材质底板、曝光与荧光边框、真实装订与三类蕾丝拼贴。最后一轮顶部1004行与用户认可的前版逐像素一致。只验证了当前样例，不能据此保证所有照片同样成功。
- 新增merge_edit.py已经在实际成片中运行并验证顶部锁定断言；原12项合成器测试再次通过。用户照片和私人测试成片不纳入开源包。
